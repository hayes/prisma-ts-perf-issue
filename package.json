{
  "name": "prisma-ts-perf-issue",
  "license": "MIT",
  "devDependencies": {
    "prisma": "^2.29.0",
    "typescript": "^4.4.0-beta"
  },
  "scripts": {
    "type": "npm run generate &&  tsc --build --extendedDiagnostics",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^2.29.0"
  }
}
